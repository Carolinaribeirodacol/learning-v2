{
    "devDependencies": {
        "typescript": "^3.4.5"
    },
    "dependencies": {
        "axios": "^0.18.0"
    },
    "scripts": {
        "start:parcel": "parcel index.html",
        "server": "json-server --watch db.json"
    }
}